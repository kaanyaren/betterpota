{
  "name": "better-pota-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker for POTA API proxy",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}